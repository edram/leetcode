use crate::leetcode::add::add;

#[test]
fn test_bad_add2() {
    // This assert would fire and test will fail.
    // Please note, that private functions can be tested too!
    assert_eq!(add(1,2), 3);
}
